---
id: coordination-p2-p8
title: "Coordination plan - P2 leader, P4 message layer, P6 board, P8 readers: four full specify → design → implement loops"
type: plan
status: proposed
owner: "@timianmalloo"
phase: "coordination"
tags: [coordination, worktrees, parallelism, leader, mail, board, readers, p2, p4, p6, p8]
links:
  - { to: proposal-owner-coordinator-subagent-coordination, rel: implements }
  - { to: coordination-compile-stage, rel: refines }
  - { to: spec-compile-stage, rel: relates-to }
  - { to: note-20260919-leadership-in-a-ref-not-the-ledger, rel: relates-to }
review-by: "2026-12-18"
review-suggested: []
summary: >-
  Four Sub-Agent tracks, each running the whole loop (/specify, /design-slice, /implement) in its own
  worktree: P4 ships the local message layer and dispatch, P6 the board over it, P2 puts leader designation in a git ref by compare-and-swap with the ratified constants
  and fences the join; P8 makes the readers consume the compile stage's fields and gives every
  prose-input skill its CO-S0 citation and the consuming skills their dispatchable stop. Authored
  paths are disjoint; the coordinator owns the shared surfaces and the join.
---

# Coordination plan — P2 leader · P4 message layer · P6 board · P8 readers

- **Scope:** build-plan items P2, P4, P6 and P8 of `proposal-owner-coordinator-subagent-coordination`, each as a full loop: spec → design → implementation with red-first tests, in the track's own tree.
- **Coordinator:** this session (Owner + Coordinator seats, S2 shape). **Sub-Agents:** one per track, `python-developer` persona, each in its own worktree created by the coordinator; each track invokes the pack's `/specify`, `/design-slice` and `/implement` skills itself.
- **Multiplier stated:** measured last time (`sp-0009`) at ≈ 1.3× tokens against the coordinator's line and a 41% wall saving on the delegated part. Paid for **genuine independence** (disjoint paths, two different scripts and doctrine sections) and **context hygiene** (each loop is three skills deep; one context should not carry both).
- **Date:** 2026-09-19 · **Tier:** T2 · **Fan-out cap:** 4 (coordinator — the GO7 width cap, each track justified below); 0 per track. **Added mid-turn at the maintainer's request:** P4 and P6.

## Layer state
| check | result | meaning |
|---|---|---|
| registry · merge driver | ok — 10 patterns; `coord-regen`, `coord-register` effective (measured 2026-09-19T16:58Z; unchanged since) | derived and register artifacts merge without a human |
| regeneration | owed in this tree until the join | the coordinator regenerates once (`sync-pack.ps1`, the four `tools/build-*.py`, `docs-graph.py derive`, `audit-log.py render`) — **including the two builders skipped twice today** |
| harness · claude | edit boundary enforcing (spike S5); delegation by the Agent tool + `coord worktree new`: **observed** on two tracks today (both returned under budget) | as before |
| sessions · worktrees | 1 active coordinator session; 5 trees (primary, this tree on `integ/p2-p8`, one held cross-platform tree, two `/private/tmp` Codex trees with uncommitted work) | the two new track trees are created off `origin/main` = `2b3a815` |

Install rule unchanged: the layer is installed once in the primary; every tree inherits it; each track's first command is `coord doctor` in its own tree.

## Artifact classes
| path / pattern | class | mechanism | coordination needed |
|---|---|---|---|
| `docs/docs-index.js`, `docs/audit/audit-data.js`, `docs/audit/index.html`, `docs/portal/portal-data.js`, `web/pack-index.js`, `docs/_site/bundle.html`, `docs/api/*.md` | derived | `coord-regen` | none — regenerated at the join |
| `docs/audit/*.jsonl`, `docs/health-history.jsonl` | register | union | none — each track's skills append their own entries |
| `docs/ai-forward-pack/**`, `.claude/**`, `.github/**`, `.grok/**`, `.agents/{skills*,hooks.json,rules/**}` | generated by `sync-pack.ps1` | none | **rule:** no track runs sync-pack |
| `docs/security/threat-model.md`, `docs/security/privacy-review.md` | authored, **shared by both loops' design steps** | lease | **rule:** tracks do NOT edit them; each reports the `documents` link it needs and the coordinator adds both and runs `docs-graph.py rollup` at the join |
| `pack/scripts/coord-core.py`, `pack/scripts/conductor-join.py`, `pack/commands/execute-with-coordination/SKILL.md`, `pack/commands/prepare-for-coordination/SKILL.md`, `pack/knowledge/agent-coordination.md`, `pack/knowledge/session-worktree-discipline.md`, `tests/docs_explorer/test_coord_leader.py`, `tests/docs_explorer/test_join_epoch.py`, `docs/specs/leader-designation.md`, `docs/design/leader-designation.md`, `docs/notes/note-*leader*.md` | authored | lease | **Track P2 only** |
| `pack/scripts/session-profile.py`, `pack/scripts/dream.py`, `pack/commands/{specify,define-architecture,design-slice,implement,investigate,ui-design,collectknowledge,forensicreview,migrate,document,code-hygiene,adopt,visualize,adddomainexperts,optimize-graph}/SKILL.md`, `pack/adapters/copilot/prompts/*.prompt.md` for those skills (if the mirror carries stage text), `tests/docs_explorer/test_readers_compile_fields.py`, `tests/docs_explorer/test_skill_co_s0_citation.py`, `pack/scripts/verify-skill-contracts.py`, `docs/specs/compile-readers.md`, `docs/design/compile-readers.md`, `docs/notes/note-*readers*.md` | authored | lease | **Track P8 only** |
| `pack/scripts/coord-mail.py` (new: `send`, `read`, `ack`, `dispatch`), `pack/adapters/hooks/*.json` + `pack/adapters/hooks/mail-doorbell.py` (new; the per-harness doorbell adapters), `pack/adapters/hooks/README.md`, `pack/scripts/pack-apply.py` (`.agents/mail/` ignore rule; `.agents/log/` tracked by default — D10), `pack/scripts/pack-doctor.py` (mail dir + doorbell status + inbox/ledger twin check), `tests/docs_explorer/test_coord_mail.py`, `tests/docs_explorer/test_mail_doorbells.py`, `docs/specs/message-layer.md`, `docs/design/message-layer.md`, `docs/notes/note-*mail*.md` | authored | lease | **Track P4 only** |
| `pack/scripts/coord-board.py` (new: `board [--follow]`, `board post`), `pack/templates/audit-explorer.template.html` (the *Messages* view), `pack/scripts/audit-log.py` (`render` emits message events into `audit-data.js` from the ledger), `tests/docs_explorer/test_coord_board.py`, `docs/specs/board.md`, `docs/design/board.md`, `docs/notes/note-*board*.md` | authored | lease | **Track P6 only** |
| `pack/adapters/INSTALL.md`, `README.md`, `pack/README.md`, `pack/OVERVIEW.md`, managed blocks, `tools/check-consistency.py`, `pack/context-budget.json`, `docs/coordination/**`, `docs/security/**`, `docs/lessons/defect-classes.md` | authored | lease | **Coordinator only** |

### Fixed contracts (GO5: no decision edge between tracks)

**Mail store (P4 writes, P6 reads; both build to this, P6 with a fixture until the join):** `.agents/mail/<session>.jsonl`, one JSON object per line, UTF-8, `newline="\n"`, append-only: `{"id": "<ULID>", "ts": "<ISO-8601 UTC>", "from": "<session>", "to": "<session>|*", "kind": "<kind>", "body": "<text ≤ 4 KiB>", "ref": "<path|audit id|null>", "ack": null}`. Kinds: `delegate · blocked · unblocked · kick · decision-request · ruling · done · ack · nack · note`. An ack is a **new line** `{"kind": "ack", "ref": "<id>"}`, never an in-place edit. **Ledger twin:** every kind except `note` and `ack`/`nack` is dual-written to the coord ledger `.agents/log/<session>.jsonl` as `{"type": "mail", "mail_id": "<id>", "kind": …, "from": …, "to": …, "ref": …}` (no body). **Doorbell contract:** a doorbell carries `{"count": <n>, "pointer": "<newest mail id>"}` and never a body. **Board read model:** the union of all `.agents/mail/*.jsonl` and the ledger's `type: mail` records, one row per mail id (who → whom · kind · ref · age · acked?), never a store; an empty corpus renders `NOT CHECKED`. `coord board post --to <session|*> "<text>"` appends a `note` (or a `ruling` with `--kind ruling --ref DR-n`) through the same writer (P6 imports P4's `append_mail()` by path; a fixture writer until the join). Constants (from Claude Code's shipped design): 50 queued, 100 held, 5-minute doorbell expiry.

**Dispatch (P4):** `coord-mail.py dispatch --harness claude-code|codex|copilot|grok|agy --brief <file> --deadline <s> --fallback <text> [--worktree <path>]` runs the harness's headless CLI under `bounded_process.py` with an `--agent-run` span, records `HARNESS_STATUS` per harness/version/date (`verified · observed-only · unsupported` — nothing not executed on this machine may read verified), refuses a dispatch without a budget or deadline, and renders `unsupported` for a channel not executed here.

**The one crossing, resolved by ownership:** P8's "consuming skills stop on an unanswered decision request" touches `execute-with-coordination` and `prepare-for-coordination`, which P2 owns. Resolution: P2 inserts the sentence P8 fixes here, verbatim, at each skill's dispatch step: *"Before dispatch, refuse a compiled prompt whose `dispatchable` is false or whose text still carries an unanswered `DR-n` line — stop with `decision request unanswered: DR-n` (CO-S0)."* P8 owns the same sentence in `optimize-graph`. Shared guards (GO14a): the three portability lints (repo root, recursive, allowlist `machine-path-ok`) and `context-budget.py skills --gate` (root `pack/commands`, ceiling 5,000 tokens; **every SKILL.md P8 edits must stay under its recorded baseline + 2%** or the addition is a citation, not a paragraph); jointly satisfiable — each track adds one sentence per skill at most.

## Tracks
| track | owns (authored) | depends on | tier | fan-out cap | budget | exit evidence | harness |
|---|---|---|---|---|---|---|---|
| **P2 — leader designation in a ref** | the P2 row above | `note-20260919-leadership-in-a-ref-not-the-ledger` (the three executed spikes), D13 constants (TTL 300 s · renew 100 s · retry 20 s · quiet 30 s), ADR-0007; ai-de read-only for the baseline (a designated human leader) | T2 | 0 | 160 tool calls · 150 min · context ceiling 400k | `docs/specs/leader-designation.md` and `docs/design/leader-designation.md` with gate records; `coord leader pin|who|renew|release|reclaim` over `refs/coord/leader` by `update-ref <ref> <new> <old>` (never `--force` with `--force-with-lease`); epoch + quiet period; `conductor-join.py` reads the epoch and refuses a lower one; `coord doctor` prints leader state; `coord metrics` counts leader-loss and contested reclaims; tests red-first: stale `old` refused · `--force` never emitted (grep test) · two concurrent pins ⇒ exactly one epoch advances · join refuses a lower epoch · quiet period honoured; the three lints exit 0; existing coord tests green | claude — enforced boundary; Agent tool observed |
| **P4 — message layer + dispatch** | the P4 row above | proposal §4b and D9 (revised); KB `data-and-constants.md` "Hook surfaces" table (Claude socket verified; `codex queue`; agy `PreInvocation injectSteps`; Copilot `agentStop` block+reason — docs-verified, execution pending); the mail store contract above; `bounded_process.py` | T2 | 0 | 160 tool calls · 150 min · context ceiling 400k | `docs/specs/message-layer.md`, `docs/design/message-layer.md` with gate records; `coord-mail.py send/read/ack` over the store contract; dual-write of state-changing kinds to the ledger; `dispatch` for `claude-code` **executed** on this machine (a real `claude -p` child under `bounded_process.py` with a budget and deadline) and `codex` executed if the CLI is present, others `unsupported`; doorbell adapters: Claude Code native message (executed), Grok/agy/Copilot hook files written to the documented contract and marked `observed-only`; `pack-doctor` checks mail dir, ignore rule, ledger tracking and inbox/ledger twins (a `delegate` or `ruling` with no twin FAILs); tests red-first: a doorbell that carries a body is refused · a dispatch without deadline refused · twin missing detected · ack is a new line; lints exit 0 | claude — enforced boundary |
| **P6 — board** | the P6 row above | D12; the mail store contract above; `docs/audit/index.html` is derived from `pack/templates/audit-explorer.template.html` (edit the template, never the derived page); `audit-log.py render` | T2 | 0 | 120 tool calls · 120 min · context ceiling 400k | `docs/specs/board.md`, `docs/design/board.md` with gate records; `coord-board.py board [--follow]` renders the read model in the terminal (plain text, no colour-only meaning) and `board post` appends through the writer; `render` emits `messages: […]` into `audit-data.js` from the ledger's `type: mail` records and the template gains a *Messages* view beside the timeline (tokens and states inherited; empty state `NOT CHECKED`); tests red-first: a row with no store line behind it fails the fixture · a post that bypasses the writer is impossible (single writer import) · empty corpus renders NOT CHECKED · `--follow` terminates on a cap; `verify-explainer-render`/a11y proof still green for the page; lints exit 0 | claude — as P2 |
| **P8 — readers and skill citations** | the P8 row above | `spec-compile-stage` (US-6, US-8), `design-compile-stage` (Telemetry, Tracks), proposal §7b.3 groups B/C, the `compile` audit kind and fields as landed (`compiled`, `compiled_from`, `edit_distance`, `dispatchable`, `mode`) | T2 | 0 | 160 tool calls · 150 min · context ceiling 400k | `docs/specs/compile-readers.md` and `docs/design/compile-readers.md` with gate records; `session-profile.py`: new findings/columns for messages-free compile telemetry (compiled-vs-run edit-distance distribution per template version, `compiled:false` share on substantive turns, contracts refused, compile duration) read from the audit log, degrading to `not recorded`; `dream.py` mines refused dispatches / compile assumptions / `compiled:false` presence as candidate classes; the twelve prose-input skills + `optimize-graph` cite CO-S0 in one sentence each; `verify-skill-contracts.py` (red first: `Runs as:` missing on 25 of 28 skills → a `Runs as:` frontmatter line added to every skill it owns, and the lint's `--self-test`); tests red-first; `context-budget.py skills --gate` green; lints exit 0 | claude — as P2 |

Both tracks: `audit-log.py start --session <track-id> --skill specify` as the brief's first line and a fresh `start` per skill; claims for the minutes of each edit; absolute paths; no `EnterWorktree`; a multi-line program is a file, then a run; gates never behind a pipe; red first; **no `sync-pack`, no `docs/security` edits, no INSTALL/README counts, no commits**.

## Serial spine
| item | why it cannot be parallel | who owns it |
|---|---|---|
| The crossing sentence in the two coordination skills | one file, one owner (P2), text fixed above | P2 |
| `Runs as:` on the three skills P2 owns (`execute-with-coordination`, `prepare-for-coordination`) and the seeded doctrine doc | P8's lint will red-flag them; P2 adds `Runs as: Coordinator` to its two skills so the join is green; the lint's own self-test and the 25-of-28 red are P8's | P2 adds two lines; P8 owns the lint |
| P6's writer is P4's `append_mail()` | one writer for the inbox (single-writer rule); P6 builds against a fixture writer with the fixed schema and the coordinator swaps in the import at the join and runs `board` over real mail | P4 owns the writer; P6 the reader |
| P4's `coord` integration line (`coord mail …` / `coord dispatch …` delegating to `coord-mail.py`) and P6's (`coord board …`) | `coord-core.py` is P2's file; two one-line delegations are added by the coordinator at the join | coordinator |
| Security rollups, counts, INSTALL rev 77, baselines, sync, derived views, verify-bundle, linear landing | shared files and generated surfaces, once | coordinator |

## Seams
| from -> to | the request | resolved by |
|---|---|---|
| P8 -> P2 | the dispatchable-stop sentence in the two coordination skills; `Runs as: Coordinator` on both | fixed in this plan; P2 applies |
| P2 -> P8 | if the leader state should appear in the profiler (leader-loss events as a finding) | a seam request; not required for either exit |
| P4 -> P6 | any deviation from the store contract (a new field, a renamed kind) | `coord request add` to P6 before writing it; the contract above is the default |
| P4, P6 -> coordinator | the `coord` delegation lines into `coord-core.py`; the `.agents/mail/` ignore rule lands through `pack-apply` (P4) and is applied to this repo's `.gitignore` by the coordinator |
| all -> coordinator | the `documents` links for the security rollups; new scripts/tests/docs for INSTALL counts (scripts 32 → 35 with `verify-skill-contracts.py`, `coord-mail.py`, `coord-board.py`; hooks + templates changed) | coordinator at the join |

## Struck tracks
| track | why it was not worth its multiplier |
|---|---|
| Split P8 into readers vs skills | the readers and the citations share one spec and the same measurement (did the citation change compile use?); two trees would need one spec |
| Split P2 into coord-core vs conductor-join | the epoch is one contract across both files; splitting makes it a permanent seam |
| P4 and P6 as one track | the board is a read model the maintainer wants to see independently of the transport; one spec each keeps the board honest about being *never a store* |
| A third track for the cross-platform P4/P5 residue | different intent (`docs/plans/cross-platform-readiness.md`), and the coordinator's line is already the critical path (sp-0009) |

## Order of operations
| # | action | cost | why now |
|---|---|---|---|
| 1 | `coord worktree new --branch impl/p2-leader --base origin/main --session p2-leader`; same for `impl/p4-mail`, `impl/p6-board`, `impl/p8-readers`; `coord doctor` in each | 3 min | isolation before spawn |
| 2 | dispatch all four in parallel with the five-part contract (width 4, transient retry 1, per-branch exit = the exit evidence, join rule = both return or the loop stops, containment = each tree) | the multiplier | independent |
| 3 | verify each return by re-running its exit evidence in its tree; commit each track on its branch | 10 min each | E16 |
| 4 | join P2, then P4, then P6, then P8 with `conductor-join.py` (join.json recount by `-k "leader or join_epoch or mail or doorbell or board or readers or co_s0 or skill_contracts or coord_core or audit_log"`); after P6 the coordinator swaps the fixture writer for the import and adds the two `coord` delegation lines | 10 min each | P6 reads P4's writer; P8's lint reads P2's `Runs as:` lines |
| 5 | coordinator: rollup links + `docs-graph.py rollup` ×2; counts; INSTALL rev 77; `context-budget.py skills --update-baseline`; sync; **all four builders**; derive; render; full pytest; `verify-bundle.ps1` on its own line | 30 min | shared surfaces |
| 6 | linear rebuild off `origin/main`, push over SSH, watch CI, `coord worktree cleanup --remove --include-unmerged`, planned vs actual, `/session-profiler` | 15 min | linear history |

## Status
| | |
|---|---|
| **Completed** | plan |
| **Remaining** | everything in the order of operations |
| **Best next action** | step 1 |
