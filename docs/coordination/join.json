{
  "_comment": [
    "The join contract for ai-forward, read by docs/ai-forward-pack/scripts/conductor-join.py.",
    "recount: the fast ring - the tests that the joined tracks own plus the audit-log suite; the full suite runs in verify-bundle before the push.",
    "regenerate: explicit, in-tree commands (never `coord regen`, which resolved against the primary checkout from a worktree once).",
    "gates: every verify-*.py in tools/ and the deployed script bundle, one exit status (run-verify-gates.py).",
    "No push_remote: main requires linear history, so the coordinator rebuilds the joined tree as a linear commit and pushes over SSH.",
    "pack_apply is excluded from the recount: test_source_repo_is_already_current needs a synced tree, and the join tree is synced only at the landing."
  ],
  "recount": [
    [
      "python3",
      "-m",
      "pytest",
      "-q",
      "tests/docs_explorer",
      "-k",
      "doctrine or requests_typed or ids_order or coord_core or coord_enforcement or leader or join_epoch or context_budget or mail or board or audit_log"
    ]
  ],
  "regenerate": [
    [
      "python3",
      "docs/ai-forward-pack/scripts/docs-graph.py",
      "derive"
    ],
    [
      "python3",
      "docs/ai-forward-pack/scripts/audit-log.py",
      "--root",
      "docs",
      "--project",
      "ai-forward",
      "render"
    ]
  ],
  "gates": [
    [
      "python3",
      "docs/ai-forward-pack/scripts/run-verify-gates.py",
      "--skip",
      "verify-explainer-render.js"
    ]
  ]
}
