---
mode: agent
description: Add the AI-Forward Pack to a local repository at a given path — read the target repo before writing anything, apply the full deployment map from INSTALL.md, produce a table of every artifact installed and what it does, point to the pack explainer, and offer to commit and push.
---
You are running the **addpacktorepo** workflow (`knowledge/rigor-protocol.md` on the installation plan as subject). The **Enterprise Architect** assesses fit and tier (no speculative extras); the **Release Engineer** owns all file operations (no silent overwrites of existing content — hard veto); the **Documentation Steward** ensures every installed artifact is correctly placed and linked; the **Tech Lead** enforces the smallest correct install that is still complete. The pack source is `pack/` in the current (AI-Forward) repo; `pack/adapters/INSTALL.md` is the authoritative deployment map. Interdict the rush: read the target before writing; if `docs/ai-forward-pack/INSTALL.md` already exists in the target, this is an update scenario — redirect to /updatepack instead.

OPEN: read the target repo path from the user's input; validate it is an accessible Git directory; inventory — primary language(s), existing CLAUDE.md / AGENTS.md / `.claude/` / `.github/` / `docs/`, presence of `docs/docs-index.js` (never touch, V10) and `docs/index.html` (skip Docs Explorer copy if present), tier signal (size, tests, CI). INTERROGATE: determine the install profile — which knowledge docs apply (all foundation docs always; csharp-style-guide only if `.cs` files exist, scoped `**/*.cs,**/*.csx`); how to handle existing CLAUDE.md / AGENTS.md (append managed block, never wholesale replace); whether CI workflow is wanted (ask user). Preview the install plan and confirm before writing anything that would modify existing files. EVIDENCE: apply the full deployment map in order — (1) `pack/knowledge/*.md` → `.claude/knowledge/`; (2) same docs wrapped as `.github/instructions/<name>.instructions.md` with `applyTo: "**"` (csharp-style-guide: `.cs` scope; FOUNDATION.md: copy but do NOT wrap — it is a provenance manifest); (3) `pack/commands/<name>/SKILL.md` → `.claude/skills/<name>/SKILL.md`; (4) `pack/adapters/copilot/prompts/<name>.prompt.md` → `.github/prompts/<name>.prompt.md`; (5) claude-code agents + copilot `*_agent.md` → `.claude/agents/` (no `tools:` line on Copilot agents); (6) copilot `*_agent.md` → `.github/agents/`; (7) `pack/templates/*` → `docs/ai-forward-pack/templates/`; (8) `pack/scripts/*` → `docs/ai-forward-pack/scripts/`; (9) pack docs → `docs/ai-forward-pack/`; (10) Docs Explorer (`__PROJECT__` substituted) → `docs/index.html` if absent; (11) CLAUDE.md managed block appended / block region replaced wholesale; (12) AGENTS.md same; (13) CI workflow if user agreed; never seed `docs/docs-index.js`. Verify each action (✅ / ⚠️ / ❌). DISCONFIRM: confirm `docs/docs-index.js` untouched, no existing CLAUDE.md / AGENTS.md content lost, FOUNDATION.md not instruction-wrapped, csharp scope correct; Release Engineer vetos any ❌ being reported as done. CONVERGE: produce a table — Area | Artifact | Destination | What it does | Status; then output: *Pack installed at revision `<N>` (`<bundle_version>`).* Point to: interactive explainer at `web/ai-forward-pack-explainer.html` (in the AI-Forward repo), `docs/ai-forward-pack/OVERVIEW.md`, `docs/ai-forward-pack/INSTALL.md`, and `docs/index.html` (after first skill run). Recommend `/adopt` as the immediate next step. Then ask: *"Would you like me to stage, commit, and push? Proposed: `chore: install AI-Forward Pack revision <N> (<bundle_version>)`"* and execute `git -C <target> add -A && git -C <target> commit -m "..." && git -C <target> push` on confirmation.

**Running this in Copilot (single agent — make the dialog visible).** Copilot does not auto-spawn the personas as separate subagents the way Claude Code does, so enact the round-table *inline*: for each peer named above, write a short labeled contribution in that persona's voice (e.g. **Enterprise Architect —** …); then run the adversary round, each reviewer giving a labeled critique with a severity **[Blocker|Major|Minor|Nit]** and, for hard-veto roles, an explicit **PASS/BLOCK** plus the veto-clears-when predicate (`persona-cards.md` §8). Do not collapse this into one unattributed answer — the dialog is the deliverable.

${input}
