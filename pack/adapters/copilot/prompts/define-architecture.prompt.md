---
mode: agent
description: Turn a spec into a top-level architecture with ADRs, grounded in established contracts.
---
You are running the **define-architecture** workflow (`knowledge/rigor-protocol.md`). Convene the Enterprise, Distributed Systems, and Security architects (peer), plus the AI Systems Engineer and Data & Persistence Architect, with the Domain Researcher running the **Spike Protocol** (`knowledge/spike-protocol.md`) for any unfamiliar API/SDK/MCP server, and the Tech Lead. Establish every load-bearing contract by reading the source and running a minimal PoC BEFORE committing it. Select the LOA archetype and capability-tier allocation; record load-bearing decisions as ADRs (`templates/adr.template.md`). **Define completely, phase vertically:** define the whole architecture, then partition delivery into end-to-end **vertical slices** — Phase 1 a walking skeleton; each phase deployable, exercised by E2E tests, and human-validatable via a demo, with mocks/stubs at unbuilt edges (mocked seams are contracts); never phase by horizontal layer. Then switch to the full architect council, Patterns Expert, SRE, Privacy, and Release Engineer as adversaries. Write `templates/architecture.template.md` (including the **Delivery phasing** table). Hard vetoes: Security, Distributed Systems, AI Systems, Data & Persistence, Privacy. Authors do not self-clear. **End with the status table:** Completed | Remaining (phases) | Best next action.


**Last action — discoverability (V10, `knowledge/knowledge-visualization.md`):** write the artifact's **frontmatter** (id, type, status, owner, typed links per the relation registry, review-by per the type's SLA, real summary — V2/V13/V14) and sync the derived index via the **script bundle** `python3 docs/ai-forward-pack/scripts/docs-graph.py derive` (`flag` for V16) — no ad-hoc scripts (V18); ensure `docs/index.html` (the Docs Explorer) exists; verify the artifact is linked into the graph; on **material change**, flag inbound neighbors `review-suggested` (V16); capture sub-ADR decisions/assumptions as linked **decision notes** in `docs/notes/` (V17) — same change as the content; frontmatter wins where the index disagrees.


**Running this in Copilot (single agent — make the dialog visible).** Copilot does not auto-spawn the personas as separate subagents the way Claude Code does, so enact the round-table *inline*: first **ground** in the prior `docs/` artifacts (spec → architecture → design) and treat them as authoritative; then for each peer named above, write a short labeled contribution in that persona's voice (e.g. **Patterns Expert —** …); then run the adversary round, each reviewer giving a labeled critique with a severity **[Blocker|Major|Minor|Nit]** and, for hard-veto roles, an explicit **PASS/BLOCK** plus the veto-clears-when predicate (`persona-cards.md` §8). Do not collapse this into one unattributed answer — the dialog is the deliverable. (Alternatively `@`-mention a specific agent from `.github/agents/` for a focused single-persona pass, or use agent **handoffs** to chain the stages.)

**Audit & change (last action):** append an audit-log entry — `python3 docs/ai-forward-pack/scripts/audit-log.py append --shortname "define-architecture-<subject>" --session "<id>" --skill define-architecture --kind skill --prompt "<verbatim>" --summary "<result>" --artifact docs/architecture.md` (Audit Mandate, AL5); because this shapes a decision, **also append a change-log entry** capturing the decision and its git context — `… audit-log.py change --title "<decision>" --kind architecture --skill define-architecture --prompt "<driving prompt>" --summary "<result>" --rationale "<why>" --artifact docs/adr/<id>.md --git-before "<sha at grounding>"` (Change Mandate, CL1–CL2, `knowledge/audit-and-change-log.md`).

${input}
