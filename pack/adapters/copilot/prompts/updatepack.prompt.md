---
mode: agent
description: Update an installed AI-Forward Pack to the latest revision from a local ai-forward clone — diff revisions in INSTALL.md, apply only the changed artifacts per the changelog, and summarise every action in a table before offering to commit and push.
---
You are running the **updatepack** workflow (`knowledge/rigor-protocol.md` applied to the update delta). The **Release Engineer** leads (owns the installation path, no silent overwrites); the **Documentation Steward** reviews every artifact landing (correct destination, managed-block integrity, V10 protection); the **Tech Lead** guards against over-application (only what `changes` lists — never diff the whole tree). Interdict the rush: two failure modes to prevent — overwriting `docs/docs-index.js` (NEVER, V10), and missing a managed-block re-paste.

OPEN: locate the pack source (from the user's input, `AI_FORWARD_PACK` env var, or a sibling `ai-forward`/`AI-Forward` directory); read the `revision`, `bundle_version`, and `changes` frontmatter from `<pack-source>/pack/adapters/INSTALL.md`; read the installed `revision` from `<target-repo>/docs/ai-forward-pack/INSTALL.md` (if absent, redirect to /addpacktorepo). Compute the delta — if 0, report up-to-date and stop; if negative, surface the anomaly to the user. INTERROGATE: classify each `changes` entry as copy | Copilot-wrap | managed-block RE-PASTE; identify protected paths and any customisation risk; surface conflicts before applying. EVIDENCE: apply each entry in changelog order — copy files to their mapped destinations per INSTALL §1, prepend `applyTo` frontmatter for Copilot knowledge wraps (csharp-style-guide scoped to `**/*.cs,**/*.csx`; FOUNDATION.md not wrapped), replace managed blocks wholesale between `AI-FORWARD-PACK:BEGIN` / `AI-FORWARD-PACK:END` markers in `CLAUDE.md` and `AGENTS.md`, verify each action (✅ / ⚠️ / ❌); then copy the source INSTALL.md to `docs/ai-forward-pack/INSTALL.md` to advance the installed revision. DISCONFIRM: confirm `docs/docs-index.js` untouched, no duplicate managed blocks, no mis-routed files; Release Engineer vetos completing while any ❌ is unresolved. CONVERGE: produce a table — Area | Artifact / File | Action | Status — state `revision <from> → <to> (<bundle_version>)`; then ask: *"Would you like me to stage, commit, and push? Proposed: `chore: update AI-Forward Pack to revision <N> (<bundle_version>)`"* and execute `git add -A && git commit -m "..." && git push` on confirmation.

**Modes.** Honour a **dry-run** (`dry run` / `--dry-run` / `preview` / `what would change`): do Stages OPEN–INTERROGATE and emit the full action table with **zero writes/commits**, then say how to apply for real. The skill is **idempotent**: `delta = 0` writes nothing ("already current"); all applies are wholesale copies + wholesale managed-block re-pastes between markers (never appends), so re-running is safe and never duplicates a block or advances past the source revision.

**Running this in Copilot (single agent — make the dialog visible).** Copilot does not auto-spawn the personas as separate subagents the way Claude Code does, so enact the round-table *inline*: for each peer named above, write a short labeled contribution in that persona's voice (e.g. **Release Engineer —** …); then run the adversary round, each reviewer giving a labeled critique with a severity **[Blocker|Major|Minor|Nit]** and, for hard-veto roles, an explicit **PASS/BLOCK** plus the veto-clears-when predicate (`persona-cards.md` §8). Do not collapse this into one unattributed answer — the dialog is the deliverable.

${input}
